# Dependencias para el script de pruebas cáusticas
# Instalar con: pip install -r requirements.txt

# Biblioteca para realizar peticiones HTTP
requests>=2.28.0

# Conector de PostgreSQL (opcional pero recomendado)
psycopg2-binary>=2.9.0

# Biblioteca para colores en terminal (opcional)
colorama>=0.4.4

# Para análisis de JSON más avanzado (opcional)
jsonschema>=4.0.0